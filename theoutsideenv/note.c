// setting up an environment variable: The environment variable is set to "C17".
// You can set it in your shell like this:
// export FROTZ=C17

//If on a Unix-like, look up the documentation for putenv(), setenv(), and unsetenv(). On Windows, see
// _putenv().